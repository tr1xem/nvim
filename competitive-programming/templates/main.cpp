// Problem: $(PROBLEM)
// Contest: $(CONTEST)
// Judge: $(JUDGE)
// URL: $(URL)
// Memory Limit: $(MEMLIM)
// Time Limit: $(TIMELIM)
// Start: $(DATE)

#include <bits/stdc++.h>

#define ll long long
#define dl double
#define REP(i, n, x) for (int i = 0; i < n; i += x)
#define rep(i, n) for (int i = 0; i < n; i++)

using namespace std;

void solve() {}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) solve();

    return 0;
}
